[tool.pytest.ini_options]
testpaths = ['tests']
markers = [
    'browser: tests that require a real browser and Semrush access',
]

[tool.ruff]
line-length = 120
target-version = 'py312'

[tool.ruff.lint]
select = ['E', 'F', 'I']
